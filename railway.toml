[build]
builder = "RAILPACK"
buildCommand = "chmod +x gradlew && ./gradlew clean build -x check -x test -Pproduction"

[deploy]
startCommand = "java -jar build/libs/insider-risk-lab-0.0.1-SNAPSHOT.jar"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
